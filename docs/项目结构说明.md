# TTPage 项目结构说明

## 📱 项目概述

这是一个使用 **Jetpack Compose** 开发的 Android 应用项目，采用 Kotlin 语言编写。项目名称是 `TTPage`，包名是 `com.xuyang.ttpage`。

## 🏗️ 项目结构详解

### 根目录结构

```
TTPage/
├── app/                          # 应用主模块（这是你主要开发的地方）
├── gradle/                       # Gradle 构建工具配置
├── build.gradle.kts             # 项目级构建配置
├── settings.gradle.kts           # 项目设置文件
├── gradle.properties            # Gradle 属性配置
├── local.properties             # 本地配置（SDK路径等）
└── gradlew / gradlew.bat        # Gradle 包装器脚本
```

### 📂 app/ 目录（核心开发目录）

```
app/
├── build.gradle.kts             # 应用模块的构建配置和依赖管理
├── proguard-rules.pro           # 代码混淆规则（发布时使用）
└── src/
    ├── main/                    # 主要源代码目录 ⭐
    │   ├── AndroidManifest.xml  # 应用清单文件（配置应用信息、权限、Activity等）
    │   ├── java/                # Kotlin/Java 源代码目录
    │   │   └── com/xuyang/ttpage/
    │   │       ├── MainActivity.kt      # 主Activity（应用入口）⭐
    │   │       ├── model/              # Model层：数据模型和仓库
    │   │       │   ├── data/
    │   │       │   │   ├── Content.kt  # 内容数据模型
    │   │       │   │   └── User.kt     # 用户数据模型
    │   │       │   └── repository/
    │   │       │       └── ContentRepository.kt  # 数据仓库
    │   │       ├── viewmodel/          # ViewModel层：状态管理
    │   │       │   ├── HomeViewModel.kt  # 首页视图模型
    │   │       │   └── UserViewModel.kt  # 用户视图模型
    │   │       ├── ui/                 # View层：UI相关代码
    │   │       │   ├── MainScreen.kt   # 主屏幕容器（包含底部导航栏）
    │   │       │   ├── theme/          # 主题配置
    │   │       │   │   ├── Color.kt    # 颜色定义
    │   │       │   │   ├── Theme.kt    # 主题配置
    │   │       │   │   └── Type.kt     # 字体样式
    │   │       │   ├── screens/        # 页面
    │   │       │   │   ├── HomeScreen.kt      # 首页（双列推荐）
    │   │       │   │   ├── DetailScreen.kt    # 详情页
    │   │       │   │   ├── ProfileScreen.kt   # 我的页面
    │   │       │   │   ├── LoginScreen.kt     # 登录页面
    │   │       │   │   └── RegisterScreen.kt  # 注册页面
    │   │       │   └── components/     # 可复用组件
    │   │       │       ├── VideoPlayer.kt          # 视频播放器组件
    │   │       │       └── BottomNavigationBar.kt   # 底部导航栏组件
    │   │       ├── navigation/         # 导航相关
    │   │       │   ├── NavGraph.kt     # 导航图配置
    │   │       │   └── Screen.kt       # 路由定义
    │   │       └── util/               # 工具类
    │   │           └── ResourceHelper.kt  # 资源工具类
    │   └── res/                  # 资源文件目录
    │       ├── drawable/         # 图片资源（包括视频封面）
    │       ├── raw/               # 原始资源（视频文件等）
    │       ├── mipmap-*/         # 应用图标（不同分辨率）
    │       ├── values/           # 值资源（颜色、字符串、样式等）
    │       │   ├── colors.xml    # 颜色资源
    │       │   ├── strings.xml   # 字符串资源
    │       │   └── themes.xml    # 主题样式
    │       └── xml/              # XML配置文件
    ├── androidTest/              # Android 测试代码
    └── test/                     # 单元测试代码
```

## 🛠️ 技术栈

### 核心技术
- **语言**: Kotlin
- **UI框架**: Jetpack Compose（声明式UI）
- **最低SDK**: Android 7.0 (API 24)
- **目标SDK**: Android 14 (API 36)
- **构建工具**: Gradle (Kotlin DSL)

### 主要依赖库
- `androidx.compose.material3` - Material 3 设计组件
- `androidx.compose.ui` - Compose UI 核心库
- `androidx.activity.compose` - Activity 与 Compose 集成
- `androidx.lifecycle.runtime.ktx` - 生命周期管理
- `androidx.lifecycle.viewmodel.compose` - ViewModel 支持
- `androidx.navigation.compose` - Navigation 导航组件
- `androidx.media3.exoplayer` - ExoPlayer 视频播放器
- `androidx.media3.ui` - ExoPlayer UI 组件

## 🚀 从哪里开始开发？

### 1. **入口文件：MainActivity.kt** ⭐⭐⭐
   **位置**: `app/src/main/java/com/xuyang/ttpage/MainActivity.kt`
   
   这是应用的入口点，包含：
   - `MainActivity` 类：应用的主Activity
   - `onCreate` 方法：应用启动时执行
   - 初始化 `MainScreen`（包含底部导航栏和导航图）

   **从这里开始**：应用启动后会显示 `MainScreen`，它包含底部导航栏和所有页面。

### 2. **应用配置：AndroidManifest.xml** ⭐⭐
   **位置**: `app/src/main/AndroidManifest.xml`
   
   配置应用的基本信息：
   - 应用名称、图标
   - Activity声明
   - 权限申请
   - 应用入口Activity

### 3. **UI主题：ui/theme/** ⭐
   **位置**: `app/src/main/java/com/xuyang/ttpage/ui/theme/`
   
   自定义应用的外观：
   - `Color.kt` - 定义颜色
   - `Theme.kt` - 定义主题
   - `Type.kt` - 定义字体样式

### 4. **资源文件：res/** ⭐
   - `res/values/strings.xml` - 文本字符串（支持多语言）
   - `res/values/colors.xml` - 颜色资源
   - `res/drawable/` - 图片资源

### 5. **依赖管理：build.gradle.kts** ⭐
   **位置**: `app/build.gradle.kts`
   
   添加新的依赖库时修改此文件。

## 📝 开发建议

### 对于初学者：

1. **先理解 MainActivity.kt**
   - 看看 `onCreate` 方法如何设置UI
   - 理解 `@Composable` 注解的作用
   - 尝试修改 `Greeting` 函数的内容

2. **学习 Jetpack Compose 基础**
   - Compose 使用函数式编程思想
   - 每个UI组件都是一个 `@Composable` 函数
   - 使用 `setContent` 来设置Activity的内容

3. **创建新的UI组件**
   - 在 `MainActivity.kt` 或新建文件中创建 `@Composable` 函数
   - 使用 Material3 组件（如 `Text`, `Button`, `Card` 等）
   - 在 `MainActivity` 中调用你的新组件

4. **组织代码结构**
   - 建议按功能模块组织代码
   - 例如：`ui/screens/`（页面）、`ui/components/`（组件）、`data/`（数据）等

### 当前项目结构（MVVM架构）：

```
app/src/main/java/com/xuyang/ttpage/
├── MainActivity.kt              # 主Activity（View层入口）
│
├── model/                        # Model层：数据和业务逻辑
│   ├── data/
│   │   ├── Content.kt           # 内容数据模型（包含视频属性）
│   │   └── User.kt              # 用户数据模型
│   └── repository/
│       └── ContentRepository.kt # 数据仓库（提供模拟数据）
│
├── viewmodel/                    # ViewModel层：状态管理
│   ├── HomeViewModel.kt         # 首页视图模型
│   └── UserViewModel.kt         # 用户视图模型（登录/注册/登出）
│
├── ui/                           # View层：UI界面
│   ├── MainScreen.kt            # 主屏幕容器（包含底部导航栏）
│   ├── theme/                    # 主题配置
│   │   ├── Color.kt
│   │   ├── Theme.kt
│   │   └── Type.kt
│   ├── screens/                  # 页面
│   │   ├── HomeScreen.kt        # 首页（双列推荐布局，支持返回顶部刷新）
│   │   ├── DetailScreen.kt      # 详情页（显示内容和视频）
│   │   ├── ProfileScreen.kt     # 我的页面（用户信息、登录/登出）
│   │   ├── LoginScreen.kt       # 登录页面
│   │   └── RegisterScreen.kt    # 注册页面
│   └── components/              # 可复用组件
│       ├── VideoPlayer.kt       # 视频播放器（播放/暂停/进度条）
│       └── BottomNavigationBar.kt  # 底部导航栏（首页+我的）
│
├── navigation/                   # 导航相关
│   ├── NavGraph.kt              # 导航图配置（包含所有页面路由）
│   └── Screen.kt                # 路由定义（Home/Profile/Login/Register/Detail）
│
└── util/                         # 工具类
    └── ResourceHelper.kt        # 资源工具类（本地资源URI）
```

### 资源文件结构：

```
app/src/main/res/
├── drawable/                     # 图片资源
│   ├── cover1.png               # 视频封面图片
│   ├── cover2.png
│   └── ...
├── raw/                          # 原始资源
│   ├── video1.mp4               # 视频文件
│   ├── video2.mp4
│   └── ...
└── values/                        # 值资源
    ├── colors.xml
    ├── strings.xml
    └── themes.xml
```

## 🎯 下一步行动

1. **运行项目**
   - 在 Android Studio 中点击运行按钮
   - 或使用命令行：`./gradlew installDebug`

2. **了解应用结构**
   - 打开 `MainActivity.kt` 查看应用入口
   - 查看 `MainScreen.kt` 了解主屏幕结构
   - 查看 `NavGraph.kt` 了解页面导航
   - 运行应用查看效果

3. **添加新功能**
   - 创建新的 Composable 函数
   - 添加按钮、输入框等组件
   - 学习 Compose 的状态管理

4. **学习资源**
   - [Jetpack Compose 官方文档](https://developer.android.com/jetpack/compose)
   - [Kotlin 官方文档](https://kotlinlang.org/docs/home.html)
   - [Android 开发者指南](https://developer.android.com/guide)

## 📦 已实现的功能

### Feature/Basic ✅
1. ✅ 首页双列推荐页面（左右两列布局）
2. ✅ 内容实体（标题、作者、时间、点赞、评论、热门标签）
3. ✅ 点击内容跳转到详情页
4. ✅ 详情页滑动返回主页

### Feature/Video ✅
1. ✅ 实体增加视频封面和视频文件属性
2. ✅ 视频播放器功能（播放、暂停、进度条）

### Feature/Test ✅
1. ✅ 单元测试（Content、Repository、ViewModel）
2. ✅ 集成测试（ResourceHelper、UI测试）

### Feature/User ✅
1. ✅ 底部导航栏（首页+我的）
2. ✅ 我的页面（显示用户信息、登录/登出）
3. ✅ 登录页面（用户名密码登录）
4. ✅ 注册页面（用户名、邮箱、密码注册）
5. ✅ 首页点击返回顶部并刷新功能

## 💡 常见问题

**Q: 如何添加新的页面？**
A: 
1. 在 `ui/screens/` 目录下创建新的 Screen Composable函数
2. 在 `navigation/Screen.kt` 中添加路由定义
3. 在 `navigation/NavGraph.kt` 中添加 `composable` 路由配置
4. 如果需要显示在底部导航栏，在 `ui/components/BottomNavigationBar.kt` 中添加导航项

**Q: 如何实现用户登录功能？**
A: 
- 使用 `UserViewModel` 管理用户状态
- 调用 `userViewModel.login(username, password)` 进行登录
- 通过 `userViewModel.currentUser` 观察当前用户状态
- 通过 `userViewModel.isLoggedIn` 观察登录状态

**Q: 如何使用本地视频和图片资源？**
A: 
- 视频文件放在 `app/src/main/res/raw/` 目录（如 `video1.mp4`）
- 图片文件放在 `app/src/main/res/drawable/` 目录（如 `cover1.png`）
- 在 `ContentRepository` 中使用资源名称（不含扩展名），如 `"video1"` 和 `"cover1"`
- 详细说明请查看 `本地资源使用说明.md`

**Q: 如何添加网络请求？**
A: 在 `build.gradle.kts` 中添加 Retrofit 或 Ktor 等网络库依赖，然后在 `model/repository/` 中实现数据获取逻辑。

**Q: 如何保存数据？**
A: 可以使用 SharedPreferences（简单数据）、Room（数据库）或 DataStore（推荐）。

**Q: 视频播放器支持哪些格式？**
A: ExoPlayer 支持 MP4、WebM、MKV 等常见视频格式。建议使用 MP4 格式以获得最佳兼容性。

**Q: 底部导航栏如何工作？**
A: 
- 底部导航栏在 `MainScreen.kt` 中定义
- 只在首页和我的页面显示
- 点击"首页"按钮：如果在首页则返回顶部并刷新，否则导航到首页
- 点击"我的"按钮：导航到我的页面

**Q: 如何实现用户登录状态管理？**
A: 
- 使用 `UserViewModel` 管理用户状态
- `currentUser` StateFlow 存储当前登录用户
- `isLoggedIn` StateFlow 表示是否已登录
- 登录/注册成功后自动更新状态

---

**祝你开发顺利！** 🎉

